apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: admin
    database: nyc_data
    editable: true
    jsonData:
      sslmode: "disable"
      postgresVersion: 1500
    secureJsonData:
      password: "password"
